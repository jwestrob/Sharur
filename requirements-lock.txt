# Sharur dependency lockfile — generated 2026-03-30
# Pinned versions from development environment (macOS, Python 3.12.5)
# For cluster deployment: conda env create -f environment.yml && pip install -e ".[dev]"
#
# NOTE: numpy<2.0 is required by pyTMHMM (Cython ABI). If pyTMHMM is not
# needed, numpy 2.x works fine with all other dependencies.

# Core
duckdb==1.2.1
faiss-cpu==1.13.2
pandas==2.2.3
numpy==2.4.3          # pyproject.toml says <2.0 for pyTMHMM; remove pyTMHMM or pin numpy<2
pyarrow==19.0.1
pydantic==2.11.7
typer==0.21.1
rich==14.2.0
requests==2.32.5
fpdf2==2.8.5

# ML / embeddings
torch==2.10.0
transformers==4.48.1
h5py==3.13.0
scipy==1.17.0

# Visualization
matplotlib==3.10.8
plotly==6.5.2
dna-features-viewer==3.1.5

# Structure prediction
biopython==1.86
esm==3.2.3

# Reports
reportlab==4.4.9

# Topology (optional — requires numpy<2.0)
# pyTMHMM==1.3.6

# Dev
pytest==9.0.2
pytest-cov==4.1.0
pytest-asyncio==0.23.8
black==26.1.0
ruff==0.14.10
mypy==1.15.0
