[project]
name = "sharur"
version = "0.1.0"
description = "Agent-driven metagenomic discovery system"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "Jacob West-Roberts"}
]
keywords = ["metagenomics", "bioinformatics", "llm", "agent", "genomics"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
]

dependencies = [
    # Core query, predicate, and ingest orchestration surface
    "duckdb>=0.9.0",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "pydantic>=2.0.0",
    "PyYAML>=6.0.0",
    "typer>=0.9.0",
    "rich>=13.0.0",
    "requests>=2.28.0",
]

[project.optional-dependencies]
parquet = [
    "pyarrow>=14.0.0",
]
vectors = [
    "faiss-cpu>=1.7.0",
    "h5py>=3.8.0",
]
embeddings = [
    "faiss-cpu>=1.7.0",
    "h5py>=3.8.0",
    "torch>=2.0.0",
    "transformers>=4.30.0",
    "scipy>=1.10.0",
]
ops = [
    "fastapi>=0.110.0",
    "uvicorn>=0.27.0",
]
visualization = [
    "matplotlib>=3.7.0",
    "dna-features-viewer>=3.1.0",
    "plotly>=5.15.0",
]
structure = [
    "biopython>=1.80",
    "esm>=3.0.0",
]
reports = [
    "fpdf2>=2.7.0",
    "reportlab>=4.0.0",
]
notebooks = [
    "jupyter>=1.0.0",
    "ipywidgets>=8.0.0",
]
all = [
    "pyarrow>=14.0.0",
    "faiss-cpu>=1.7.0",
    "h5py>=3.8.0",
    "torch>=2.0.0",
    "transformers>=4.30.0",
    "scipy>=1.10.0",
    "fastapi>=0.110.0",
    "uvicorn>=0.27.0",
    "matplotlib>=3.7.0",
    "dna-features-viewer>=3.1.0",
    "plotly>=5.15.0",
    "biopython>=1.80",
    "esm>=3.0.0",
    "fpdf2>=2.7.0",
    "reportlab>=4.0.0",
    "jupyter>=1.0.0",
    "ipywidgets>=8.0.0",
]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "pytest-asyncio>=0.21.0",
    "httpx>=0.27.0",
    "build>=1.2.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
    "pre-commit>=3.0.0",
]

[project.urls]
Homepage = "https://github.com/jwestrob/Sharur"
Documentation = "https://github.com/jwestrob/Sharur#readme"
Repository = "https://github.com/jwestrob/Sharur"
Issues = "https://github.com/jwestrob/Sharur/issues"

[project.scripts]
sharur = "sharur.cli:main"
sharur-ingest = "sharur.ingest_cli:main"
sharur-ops = "sharur.ops_cli:main"
sharur-query = "sharur.query_cli:main"
sharur-atlas = "sharur.atlas_cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["sharur"]

[tool.hatch.build.targets.wheel.force-include]
"config/predicates_v2" = "sharur/predicates_v2/config"
"src/ingest/00_prepare_inputs.py" = "sharur/ingest/stages/00_prepare_inputs.py"
"src/ingest/01_run_quast.py" = "sharur/ingest/stages/01_run_quast.py"
"src/ingest/02_dfast_qc.py" = "sharur/ingest/stages/02_dfast_qc.py"
"src/ingest/03_prodigal.py" = "sharur/ingest/stages/03_prodigal.py"
"src/ingest/04_astra_scan.py" = "sharur/ingest/stages/04_astra_scan.py"
"src/ingest/06_esm2_embeddings.py" = "sharur/ingest/stages/06_esm2_embeddings.py"
"src/ingest/07_build_knowledge_base.py" = "sharur/ingest/stages/07_build_knowledge_base.py"
"src/ingest/dbcan_cazyme.py" = "sharur/ingest/stages/dbcan_cazyme.py"
"src/ingest/gecco_bgc.py" = "sharur/ingest/stages/gecco_bgc.py"
"src/ingest/minced_crispr.py" = "sharur/ingest/stages/minced_crispr.py"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-v",
    "--cov=sharur",
    "--cov-report=term-missing",
    "--cov-report=html:coverage_html",
    "-x",  # Stop on first failure
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
]

[tool.coverage.run]
source = ["sharur"]
branch = true
omit = [
    "sharur/__main__.py",
    "sharur/cli.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.:",
]

[tool.black]
line-length = 100
target-version = ["py310", "py311", "py312"]
include = '\.pyi?$'
exclude = '''
/(
    \.git
    | \.hatch
    | \.mypy_cache
    | \.pytest_cache
    | \.venv
    | _build
    | build
    | dist
)/
'''

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "F",      # pyflakes
    "I",      # isort
    "N",      # pep8-naming
    "W",      # pycodestyle warnings
    "UP",     # pyupgrade
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "DTZ",    # flake8-datetimez
    "T10",    # flake8-debugger
    "ISC",    # flake8-implicit-str-concat
    "ICN",    # flake8-import-conventions
    "PIE",    # flake8-pie
    "PT",     # flake8-pytest-style
    "RSE",    # flake8-raise
    "RET",    # flake8-return
    "SIM",    # flake8-simplify
    "TID",    # flake8-tidy-imports
    "TCH",    # flake8-type-checking
    "ARG",    # flake8-unused-arguments
    "PGH",    # pygrep-hooks
    "PLC",    # pylint convention
    "PLE",    # pylint error
    "PLW",    # pylint warning
    "RUF",    # ruff-specific
]
ignore = [
    "E501",   # line too long (handled by black)
    "B008",   # do not perform function calls in argument defaults
    "C901",   # too complex
    "PLR0913", # too many arguments
    "ARG001", # unused function argument
    "ARG002", # unused method argument
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["ARG", "PLR2004"]

[tool.ruff.lint.isort]
known-first-party = ["sharur"]
force-single-line = false
lines-after-imports = 2

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_ignores = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
strict_optional = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_configs = true

[[tool.mypy.overrides]]
module = [
    "duckdb.*",
    "faiss.*",
    "pyarrow.*",
]
ignore_missing_imports = true
